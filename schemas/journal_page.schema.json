{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$title": "Journal Page Schema",
  "type": "object",
  "required": ["journal_page"],
  "properties": {
    "journal_page": {
      "type": "object",
      "additionalProperties": false,
      "required": ["page_id", "date", "place_id", "entry_type", "mood", "need", "ingredients", "prompt", "body"],
      "properties": {
        "page_id": {
          "type": "string",
          "pattern": "^[a-z0-9_]+$"
        },
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
        },
        "place_id": {
          "type": "string",
          "pattern": "^[a-z0-9_]+$"
        },
        "entry_type": {
          "type": "string",
          "enum": ["tea", "spell", "field_note", "dream_fragment"]
        },
        "mood": {
          "type": "string",
          "minLength": 1
        },
        "need": {
          "type": "string",
          "minLength": 1
        },
        "ingredients": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string" }
        },
        "prompt": {
          "type": "string",
          "minLength": 1
        },
        "body": {
          "type": "string",
          "minLength": 1
        },
        "tags": {
          "type": "array",
          "items": { "type": "string" },
          "default": []
        }
      }
    }
  }
}